<section>
    <%= form_with url: result_test_path, method: :get, local: true do |f| %>
        <table class="index">
            <thead>
				<tr>
					<th>単語</th>	
					<th>解答</th>
                    <th>答え</th>
				</tr>
			</thead>
            <tbody>
                <% @words.each do |v|  %>
                    <tr>
                        <td><%= v[0] %></td>
                        <td><%= v[1] %></td>
                        <td><%= v[2] %></td>
                    </tr>                
                <% end %>
            </tbody>
        </table>
        <div class="flex align-center">
            <%= f.submit 'Submit a result', class: "btn btn-primary" %>
        </div>
    <% end %>
</section>